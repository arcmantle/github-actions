{
	"name": "@arcmantle/detect-changes-action",
	"version": "1.0.0",
	"private": true,
	"description": "GitHub Action to detect which packages have changed",
	"type": "module",
	"main": "dist/main.js",
	"scripts": {
		"build": "tsdown"
	},
	"dependencies": {
		"@actions/core": "^1.11.1"
	}
}
